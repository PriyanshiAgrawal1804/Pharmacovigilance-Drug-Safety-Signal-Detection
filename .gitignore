# Generated / reproducible outputs — keep raw synthetic data + figures in
# the repo for reviewers, but processed intermediates regenerate via
# `Rscript run_pipeline.R`
data/processed/

# R
.Rhistory
.RData
.Rproj.user/

# OS
.DS_Store
